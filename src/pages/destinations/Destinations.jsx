import Categories from '../../components/categories/Categories';
import { destinations } from '../../mock/mock';

const Destinations = () => <Categories categories={destinations} />;

export default Destinations;
